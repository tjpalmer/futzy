ROS bridge for the 2D RoboCup Soccer Server, making research-oriented use more
convenient.

The history is that I had trouble finding a simple Python client library for the
RoboCup Soccer Server (rcss), so I started one. Then I felt more and more like
having ROS access would be more useful, so I went that direction.
